---
bump: minor
---

### Added

- Make `default-features = false` expose a lightweight deterministic merge API
  without compiling the provider, server, CLI, `web-capture`, or native-TLS
  dependency graph.
